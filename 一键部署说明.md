# VoHive Fork 一键部署说明

## 两个仓库分别做什么

- `zhangsan-nb/vohive`：完整源码仓，能够编译 VoHive。
- `zhangsan-nb/vohive-release`：安装入口和运维文档仓，本身不含程序二进制。

截至 2026-07-14，这两个 fork 都没有 GitHub Release 附件。因此旧安装脚本即使改掉原作者用户名，仍会在“下载二进制”处得到 404。

现已找回原作者最后保留的三个真实二进制，版本标识为 `v1.5.5-10-gf9eb85d`，分别适用于 amd64、arm64、armv7。应在 `zhangsan-nb/vohive-release` 新建同名 Release，并把三个原始文件和 `SHA256SUMS` 作为附件上传。不要改动文件名，也不要把三个文件压成一个 ZIP 后只上传 ZIP。

新版 `install.sh` 已改为：

1. 先在 `zhangsan-nb/vohive-release` 查找适合当前 CPU 的最新二进制；
2. 再到 `zhangsan-nb/vohive` 的 Release 中查找；
3. 两处都没有时，从 `zhangsan-nb/vohive` 的 `main` 分支自动编译并安装；
4. 更新时保留现有配置，并把旧二进制备份为 `/opt/vohive/bin/vohive.bak`；
5. 首次安装生成随机 Web 密码，不再使用危险的 `admin/admin`。

## 一键安装（Debian / Ubuntu）

先把新版 `install.sh` 提交到 `zhangsan-nb/vohive-release` 的 `master` 分支，然后在 Debian 服务器执行：

```bash
curl -fsSL https://raw.githubusercontent.com/zhangsan-nb/vohive-release/master/install.sh | sudo bash
```

如果已按上述要求发布 `v1.5.5-10-gf9eb85d` Release，脚本会直接下载当前 CPU 对应的附件并校验 SHA-256，不再现场编译。没有发布附件时才会回退到源码构建。

## 常用命令

安装指定源码分支：

```bash
curl -fsSL https://raw.githubusercontent.com/zhangsan-nb/vohive-release/master/install.sh | sudo bash -s -- --source-ref main
```

安装指定 Release：

```bash
curl -fsSL https://raw.githubusercontent.com/zhangsan-nb/vohive-release/master/install.sh | sudo bash -s -- --version v1.5.3
```

只接受预编译二进制（不存在就退出）：

```bash
curl -fsSL https://raw.githubusercontent.com/zhangsan-nb/vohive-release/master/install.sh | sudo bash -s -- --binary-only
```

查看状态与日志：

```bash
systemctl status vohive --no-pager
journalctl -u vohive -f
```

重启：

```bash
sudo systemctl restart vohive
```

后台地址：

```text
http://服务器局域网IP:7575
```

用户名为 `admin`，首次生成的随机密码会在安装结束时显示。不要把 7575 端口直接映射到公网。

## 大疆 4G 模块说明

安装脚本只负责 VoHive 程序，不会自动修改模块 USB 身份、切换 USBNET 模式或停用 `ModemManager`。这些操作可能影响现有联网，应该先确认模块的 VID/PID、AT 口和当前拨号方式，再单独处理。

VoWiFi 是否可用还取决于运营商、SIM 权限、IMS 配置和网络环境；VoHive 安装成功不等于 VoWiFi 一定能注册。
